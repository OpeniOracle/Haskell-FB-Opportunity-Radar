
# Generated by db/hosted-plan.mjs: migrations with \ir expanded for transport
# through the Supabase management API. Derived from db/migrations, never edited.
db/.hosted/

# npm creates a stray root node_modules when a command is run from the
# repository root instead of app/. There is no root package.json, so it is
# always an accident — and an untracked directory would trip the clean-tree
# guard in the hosted-validation scripts.
node_modules/
